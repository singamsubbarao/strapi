name: sample project CI

on:
  push:
    branches: master
  pull_request:
    branches: test

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Hello from sample project
